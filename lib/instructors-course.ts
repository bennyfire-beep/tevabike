// Shared config for the "קורס מדריכי רכיבה טכנית" landing page — imported by
// both the page (client) and its registration API route (server) so the two
// can never drift.
//
// The course dates aren't confirmed yet — fill in COURSE_DATES_LABEL once the
// college sets them, and both the page and the confirmation email will pick
// it up automatically.

export const COURSE_DATES_LABEL = "יפורסמו בהמשך"
export const COLLEGE_REGISTRATION_LINK = "https://fill.tofsy.co.il/BvjFJw"
export const COLLEGE_PHONE = "04-6749202"
